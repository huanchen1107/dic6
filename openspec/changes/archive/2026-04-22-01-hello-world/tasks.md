# Implementation Tasks

- [x] 1. **Create index.html**
  - [x] Create a basic HTML5 boilerplate.
  - [x] Add a `<main>` container with a `<h1>` for the greeting and a `<button>`.
  - [x] Link `style.css` and `app.js`.

- [x] 2. **Create style.css**
  - [x] Implement a responsive, centered layout using Flexbox or Grid on the body.
  - [x] Add styling for the card container, heading, and button (colors, padding, borders, hover states).

- [x] 3. **Create app.js**
  - [x] Select the button and heading elements.
  - [x] Add a click event listener to the button.
  - [x] Update the heading text when the button is clicked.

- [x] 4. **Import existing repository content**
  - [x] Pull everything from `https://github.com/Chao0228/D4PG_midterm.git`.
  - [x] Merge contents into the current repository (including PDFs and video).
